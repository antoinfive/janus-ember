{{yield}}
<nav class="navbar navbar-default fix-navbar">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{link-to 'Janus' 'dashboard' class="navbar-brand"}}
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        {{#if session.isAuthenticated}}
          <li class="active"><a{{action 'invalidateSession'}}>Logout<span class="sr-only">(current)</span></a></li>
          <li>{{link-to 'Dashboard' 'dashboard'}}</li>
          <li>{{link-to 'Projects' 'projects'}}</li>
          <li>{{link-to 'Snippets' 'snippets'}}</li>
          <li>{{link-to 'Bookmarks' 'bookmarks'}}</li>
        {{else}}
          <li class="active">{{#link-to 'login'}}Login{{/link-to}}</li>
          <li class="active">{{#link-to 'user.signup'}}Sign Up{{/link-to}}</li>

        {{/if}}
        <!-- <li class="active"><a href="#">Link <span class="sr-only">(current)</span></a></li>
        <li><a href="#">Link</a></li> -->
      </ul>
    </div><!-- /.navbar-collapse -->
    <!-- {{#if session.isAuthenticated}}
      <ul class="nav navbar-nav navbar-right">
        <li>Welcome {{currentUser}}</li>
      </ul>
    {{/if}} -->
  </div><!-- /.container-fluid -->
</nav>
